<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <!-- =========================================================
         Define log patterns
         ========================================================= -->
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>
    <property name="LOG_FILE_PATTERN" value="logs/university-app-%d{yyyy-MM-dd}.log"/>

    <!-- =========================================================
         Console Appender
         ========================================================= -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- =========================================================
         File Appender with rolling
         ========================================================= -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/university-app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>logs/university-app-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- =========================================================
         JSON Appender (Optional, for ELK/Kafka central logging)
         Uncomment if using logstash or JSON logging
         ========================================================= -->
    <!--
    <appender name="JSON" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>logstash-host:5000</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
    </appender>
    -->

    <!-- =========================================================
         Log Levels
         ========================================================= -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <!-- <appender-ref ref="JSON"/> Uncomment for centralized logging -->
    </root>

    <!-- Specific package logging levels -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="com.example.university" level="DEBUG"/>
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="org.apache.cassandra" level="WARN"/>
    <logger name="io.lettuce" level="WARN"/>

    <!-- Trace logging for debugging Resilience4j if needed -->
    <logger name="io.github.resilience4j" level="DEBUG"/>

</configuration>
